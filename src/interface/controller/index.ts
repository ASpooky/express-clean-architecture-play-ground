import type { Request,Response } from "express";

const IndexController = (req:Request,res:Response) =>{
    //　ここでリクエストからパラメータなどをひきだしたりする
    // usecaseを呼び出したりする
    // preseterを呼び出したりする
    // viewModelを返す。
}